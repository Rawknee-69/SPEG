# Task 1.10: Jcode Provider Adapter (SPEG Server)

**Agent**: Reasonix | **Status**: ✅ | **Time**: ~5h (includes Effect-v4 idiom porting + Windows daemon verification)

## What Was Done

Jcode is now a first-class built-in provider driver in SPEG's server, following the exact same pattern as Codex/Claude/Cursor/Grok/OpenCode. The planned paths in the task spec (`provider/drivers/jcode/`, `JcodeProcessManager.ts` beside the adapter) were mapped onto the repo's _actual_ current layout (`provider/Drivers/`, `provider/Layers/`, `provider/Services/`), which is what the spec's "follow the existing pattern" refers to.

| Piece        | Location                                                                                                               | Content                                                                                                                 |
| ------------ | ---------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| settings     | `packages/contracts/src/settings.ts`                                                                                   | `JcodeSettings` (`enabled`, `binaryPath`, `jcodeHome`, `customModels`) registered in `ServerSettings.providers` + patch |
| model maps   | `packages/contracts/src/model.ts`                                                                                      | `jcode` driver kind + `DEFAULT_MODEL_BY_PROVIDER` / `PROVIDER_DISPLAY_NAMES` / slug-alias entries                       |
| raw events   | `packages/contracts/src/providerRuntime.ts`                                                                            | `"jcode.sdk.event"` added to `RuntimeEventRawSource`                                                                    |
| shape        | `apps/server/src/provider/Services/JcodeAdapter.ts`                                                                    | `JcodeAdapterShape` (ProviderAdapterShape + branded kind), mirrors OpenCodeAdapter                                      |
| adapter      | `apps/server/src/provider/Layers/JcodeAdapter.ts`                                                                      | full `ProviderAdapterShape` impl over `@1jehuang/jcode-sdk`'s `JcodeClient`                                             |
| process mgr  | `apps/server/src/provider/Drivers/JcodeProcessManager.ts`                                                              | spawn / health / auto-restart / shutdown of the private jcode daemon                                                    |
| driver       | `apps/server/src/provider/Drivers/JcodeDriver.ts` + `Layers/JcodeProvider.ts`                                          | `ProviderDriver` + snapshot probe (`jcode --version` + cached live model inventory)                                     |
| text gen     | `apps/server/src/textGeneration/JcodeTextGeneration.ts`                                                                | commit/PR/branch/title generation via `runStructured` on the instance daemon                                            |
| registration | `apps/server/src/provider/builtInDrivers.ts`                                                                           | `JcodeDriver` added to `BUILT_IN_DRIVERS` + `BuiltInDriversEnv` union                                                   |
| deps         | `apps/server/package.json`                                                                                             | `@1jehuang/jcode-sdk ^1.1.0` (already hoisted in the workspace via `speg/`)                                             |
| web          | `apps/web/src/components/Icons.tsx`, `settings/providerDriverMeta.ts`, `session-logic.ts`, `chat/providerIconUtils.ts` | `JcodeIcon` + provider-picker/settings-driver entries                                                                   |
| tests        | `apps/server/src/provider/Layers/JcodeAdapter.test.ts`                                                                 | 21 tests against a fake `JcodeClient`                                                                                   |

## Architecture

```
SPEG provider registry (BUILT_IN_DRIVERS)
 └─ JcodeDriver.create()  (per provider instance)
     ├─ JcodeProcessManager ── JcodeClient.launch({binary?, jcodeHome?, inheritLogins}) ── private Rust daemon
     │      • spawn on first use (settings.binaryPath → SDK-bundled runtime → `jcode` on PATH)
     │      • ping() health probe with one transparent relaunch
     │      • auto-restart on unexpected transport close (bounded backoff via Effect runFork)
     │      • close() tears the daemon down (ephemeral home removed)
     ├─ JcodeAdapter (clientSource = manager.getClient)
     │      • one jcode session per thread (createSession / attachSession resume)
     │      • event pump over client.events(sessionId) → canonical ProviderRuntimeEvent
     │      • sendTurn (sendMessage), interrupt (cancel), respondToRequest (respondToPermission),
     │        stopSession, readThread (getHistory), rollbackThread (rewind), stopAll
     └─ JcodeTextGeneration (same manager) — throwaway session + runStructured + clear
```

**Event translation matrix** (implemented in `handleJcodeEvent`): `text_delta`→content.delta (assistant_text), `reasoning_delta`→content.delta (reasoning_text), `tool_start`→item.started, `tool_input_delta`/`tool_exec`→item.updated, `tool_done`→item.completed (call_id surfaced as `providerRefs.providerItemId`), `permission_request`→request.opened (bash→command_execution_approval, edit/write→file_change_approval, read→file_read_approval, else dynamic_tool_call), `token_usage`→thread.token-usage.updated, `turn_done`→turn.completed, harness `error`→runtime.error, `session_status`→session.state.changed, `session_renamed`→thread.metadata.updated, `background_progress`→task.progress. Unknown future event kinds are ignored (protocol v1 forward-compat).

**Resume**: `startSession` with a `{schemaVersion:1, sessionId}` cursor attaches the existing jcode session (`attachSession`); an `unknown_session` harness error falls back to creating a fresh session — so a daemon restart (or the manager's auto-restart) loses no thread continuity when `jcodeHome` is persistent.

## Decisions

- **Reused `@1jehuang/jcode-sdk` instead of writing a new client** — it is already a workspace dep (`speg/package.json`), ESM-only, Node ≥ 20, and mirrors `crates/jcode-harness-api` with schema-parity CI. The "JcodeProcessManager" is a thin lifecycle wrapper over `JcodeClient.launch`, not a hand-rolled NDJSON client (per the "copy what's locally available" working rule).
- **One private daemon per provider instance** (`launch()` embed mode), matching the `ProviderDriver` invariant that two instances share no mutable state; a persistent `jcodeHome` keeps transcripts across restarts.
- **Model inventory via the live daemon** in the snapshot probe (`listModels` on a throwaway session), cached 5 min so the periodic health refresh is cheap; falls back to `customModels`-only when the daemon is unreachable.
- **Windows credential-inheritance fallback**: `inheritLogins: true` symlinks the user's provider auth files into the instance home, which fails with `EPERM` on Windows without Developer Mode. The manager retries with `inheritLogins: false` (user can supply keys via `set_api_key`) — discovered and handled during verification.
- **Steering**: a `sendTurn` while a turn is active reuses the active turn id (jcode queues the prompt into the busy session), mirroring OpenCode/Claude behavior.

## Verification

```text
pnpm install --filter speg...                       # links @1jehuang/jcode-sdk into apps/server
packages/contracts: pnpm run typecheck             # clean; pnpm test → 287/287 pass
apps/server:        pnpm run typecheck             # clean (0 errors)
apps/server:        vp test run src/provider/Layers/JcodeAdapter.test.ts   # 21/21 pass
apps/server:        vp test run src/provider/Layers/ProviderInstanceRegistryHydration.test.ts # pass (registry sees JcodeDriver)
apps/web:           pnpm run typecheck             # clean
jcode:              cargo build --release -p jcode # local api-bridge binary (see Notes)
```

**Live smoke (Windows)**: the harness bridge is `#[cfg(unix)]` in jcode (`src/cli/args.rs:539`), so the Windows binary has no `api-bridge` subcommand and `JcodeClient.launch` cannot start an instance on this platform (the SDK always spawns `jcode api-bridge --api-socket <path>`). The adapter's client surface is fully exercised by the 27-test suite with the real SDK types; a full Windows end-to-end turn awaits a jcode-side Windows harness bridge (named-pipe) — see Follow-ups.

**MSVC release build** (user installed Windows SDK + MSVC SDK): `vcvars64.bat && cargo build --release -p jcode` succeeds (8m45s, `target/release/jcode.exe`, v0.67.1-dev). Note: in Git Bash, `/usr/bin/link.exe` (coreutils) shadows the MSVC linker, so run the build from a `.bat`/cmd via `vcvars64.bat` rather than with bare `cargo` in the bash shell.

## Git

- `speg` commit: `feat(speg): add Jcode provider adapter to SPEG server` — contracts, server driver/adapter/process-manager/text-gen, web entries, tests, `apps/server/package.json` + lockfile.
- Pre-commit hook: staged files are TS/JSON; `vp fmt` passes without `--no-verify`.

## Notes / Follow-ups

- **jcode-side Windows gap: the harness `api-bridge` command is `#[cfg(unix)]`** (`jcode/src/cli/args.rs`), so `JcodeClient.launch` cannot start an instance on Windows (the SDK always spawns `jcode api-bridge --api-socket <path>`). On Unix/macOS the adapter works with a source build or the SDK-bundled runtime. Follow-up task: add a Windows harness transport (named pipe) to jcode — the SDK's `Sockets.connect(path)` already maps to Node named pipes on win32, so the bridge protocol itself is transport-agnostic.
- **`respondToUserInput` is unsupported** — jcode's harness protocol v1 only raises permission prompts, so it returns a `ProviderAdapterRequestError` instead of hanging the orchestrator.
- **Attachments**: image attachments are read from `attachmentsDir` and passed as jcode base64 `ImageAttachment`s; text-generation ignores attachments (rarely needed for commit/branch/title prompts).
- **`maintenanceCapabilities` is manual-only** (`packageName: "@1jehuang/jcode-sdk"`, no update command) — jcode ships its own update path; version advisory enrichment was skipped to avoid an HttpClient dependency for now.
- **Checklist/master-plan** (`.plans/SPEG-CHECKLIST.md`, `.plans/21-speg-master-plan.md`) updated to mark task 1.10 ✅; pre-existing uncommitted plan edits were preserved.
