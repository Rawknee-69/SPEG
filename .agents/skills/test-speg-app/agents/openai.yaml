interface:
  display_name: "SPEG App Testing"
  short_description: "Launch and retain isolated SPEG test environments"
  default_prompt: "Use $test-speg-app to launch an isolated SPEG environment and iteratively test it in the browser while preserving state."
